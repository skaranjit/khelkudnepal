<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- typeof title !== 'undefined' ? title + ' | Khelkud Nepal' : 'Khelkud Nepal - Latest Sports News' %></title>
    <!-- Latest Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <!-- Custom styles for fixing dropdown visibility -->
    <style>
        /* Ensure dropdowns are visible and positioned properly */
        .dropdown-menu {
            display: none;
            position: absolute;
            z-index: 1000;
            min-width: 10rem;
            padding: 0.5rem 0;
            margin: 0;
            background-color: #fff;
            border: 1px solid rgba(0,0,0,.15);
            border-radius: 0.25rem;
        }
        .dropdown-menu.show {
            display: block !important;
        }
        /* Fix for dropdown items */
        .dropdown-item {
            display: block;
            width: 100%;
            padding: 0.25rem 1.5rem;
            clear: both;
            text-align: inherit;
            white-space: nowrap;
            background-color: transparent;
            border: 0;
        }
        .dropdown-item:hover, .dropdown-item:focus {
            color: #16181b;
            text-decoration: none;
            background-color: #f8f9fa;
        }
        /* Maintenance banner styles */
        .maintenance-banner {
            background-color: #f8d7da;
            color: #721c24;
            padding: 10px 0;
            text-align: center;
            font-weight: bold;
        }
    </style>
    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Custom dropdown fix -->
    <script src="/js/dropdown-fix.js"></script>
</head>
<body>
    <% if (locals.settings && settings.advanced && settings.advanced.maintenanceMode) { %>
    <div class="maintenance-banner">
        <%= settings.advanced.maintenanceMessage || 'We are currently performing maintenance. Please check back soon.' %>
    </div>
    <% } %>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="/">
                    <img src="<%= locals.settings && settings.site && settings.site.logo ? settings.site.logo : '/images/logo.png' %>" alt="Khelkud Nepal" height="40">
                    Khelkud Nepal
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link <%= typeof activeNav !== 'undefined' && activeNav === 'home' ? 'active' : '' %>" href="/">Home</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Categories
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/category/cricket">Cricket</a></li>
                                <li><a class="dropdown-item" href="/category/football">Football</a></li>
                                <li><a class="dropdown-item" href="/category/basketball">Basketball</a></li>
                                <li><a class="dropdown-item" href="/category/volleyball">Volleyball</a></li>
                                <li><a class="dropdown-item" href="/category/other">Other Sports</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/categories">All Categories</a></li>
                            </ul>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= typeof activeNav !== 'undefined' && activeNav === 'latest' ? 'active' : '' %>" href="/latest">Latest News</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= typeof activeNav !== 'undefined' && activeNav === 'trending' ? 'active' : '' %>" href="/trending">Trending</a>
                        </li>
                    </ul>
                    <form class="d-flex me-2" id="search-form" action="/search" method="GET">
                        <input class="form-control me-2" type="search" placeholder="Search news..." aria-label="Search" name="q" id="search-input">
                        <button class="btn btn-outline-light" type="submit">Search</button>
                    </form>
                    <div class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle"></i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <% if (locals.user) { %>
                                <li><span class="dropdown-item-text">Hello, <%= user.username %></span></li>
                                <li><hr class="dropdown-divider"></li>
                                <% if (user.role === 'admin') { %>
                                    <li><a class="dropdown-item" href="/admin/dashboard">Admin Dashboard</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                <% } %>
                                <li><a class="dropdown-item" href="/profile">My Profile</a></li>
                                <li><a class="dropdown-item logout-link" href="javascript:void(0)">Logout</a></li>
                            <% } else { %>
                                <li><a class="dropdown-item" href="/login">Login</a></li>
                                <li><a class="dropdown-item" href="/register">Register</a></li>
                            <% } %>
                        </ul>
                    </div>
                </div>
            </div>
        </nav>
    </header>
    <main>
    </main>
</body>
</html> 